import 'dotenv/config';
//import { get } from 'env-var';

export const envs = {
  //PORT: get('PORT').required().asPortNumber(),
  //JWT_SEED: get('JWT_SEED').required().asString(),

  PORT: 3000,
  JWT_SEED: 'ESTA_ES_MI_SEED_SECRETA_123456'
};
